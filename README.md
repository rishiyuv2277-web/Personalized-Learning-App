
  # Personalized Learning App Prototype

  This is a code bundle for Personalized Learning App Prototype. The original project is available at https://personalized-learning-app-beta.vercel.app/
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
